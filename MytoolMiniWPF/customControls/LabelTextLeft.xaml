<UserControl x:Class="MytoolMiniWPF.customControls.LabelTextLeft"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MytoolMiniWPF.customControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="180">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="leftText" Grid.Column="0"  FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Stretch" Text="{Binding left_text}" TextAlignment="Right"/>
        <TextBox x:Name="inputText" Grid.Column="1" FontSize="14" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Text="{Binding value}" Margin="5 5 5 5" Style="{StaticResource MaterialDesignTextBox}" Cursor="IBeam"/>
    </Grid>
</UserControl>
